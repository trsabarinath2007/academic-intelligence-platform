import React,{useEffect,useState}from"react";import Layout from"../components/Layout";import{Card,LoadingState,ErrorState}from"../components/UI";import{apiGet,currentUser}from"../api";
export default function StudentProfile(){const[d,setD]=useState(null),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{apiGet("/students/profile").then(x=>setD(x.student)).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);if(loading)return <Layout title="Profile"><LoadingState/></Layout>;if(error)return <Layout title="Profile"><ErrorState message={error}/></Layout>;const u=d?.user||currentUser();return <Layout title="Profile"><div className="page-header"><div><div className="eyebrow">Student Portal</div><h1>My Profile</h1><p>View your academic and account information.</p></div></div><Card><div className="profile-card"><div className="profile-avatar">{(u.name||"S").charAt(0)}</div><div className="profile-info"><h2>{u.name||"Student"}</h2><p>{u.email||""}</p></div></div><div className="info-grid" style={{marginTop:18}}><div className="info-box"><span>Student ID</span><strong>{d?.studentId}</strong></div><div className="info-box"><span>Department</span><strong>{d?.department}</strong></div><div className="info-box"><span>Semester</span><strong>{d?.semester}</strong></div><div className="info-box"><span>Section</span><strong>{d?.section}</strong></div></div></Card></Layout>}
