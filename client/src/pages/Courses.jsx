import React,{useEffect,useState}from"react";import Layout from"../components/Layout";import{Card,SearchBox,LoadingState,ErrorState,EmptyState,StatusPill}from"../components/UI";import{apiGet}from"../api";
export default function Courses(){const[r,setR]=useState([]),[q,setQ]=useState(""),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{apiGet("/courses").then(d=>setR(d.courses||[])).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);const f=r.filter(x=>`${x.courseCode} ${x.courseName} ${x.department}`.toLowerCase().includes(q.toLowerCase()));return <Layout title="Courses"><div className="page-header"><div><div className="eyebrow">Student Portal</div><h1>My Courses</h1><p>Courses available for your academic semester.</p></div></div><div className="toolbar"><SearchBox value={q} onChange={setQ} placeholder="Search courses..."/></div><Card>{loading?<LoadingState/>:error?<ErrorState message={error}/>:f.length===0?<EmptyState text="No courses found"/>:<div style={{overflowX:"auto"}}><table className="data-table"><thead><tr><th>Course</th><th>Department</th><th>Semester</th><th>Credits</th><th>Status</th></tr></thead><tbody>{f.map(x=><tr key={x._id}><td><div className="row-title">{x.courseCode}</div><div className="row-sub">{x.courseName}</div></td><td>{x.department}</td><td>{x.semester}</td><td>{x.credits}</td><td><StatusPill tone="good">Active</StatusPill></td></tr>)}</tbody></table></div>}</Card></Layout>}
