import React,{useEffect,useState}from"react";import Layout from"../components/Layout";import{Card,LoadingState,ErrorState,EmptyState,StatusPill}from"../components/UI";import{apiGet}from"../api";
export default function AcademicRecords(){const[r,setR]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{apiGet("/academic-records/student").then(d=>setR(d.records||d.academicRecords||[])).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);return <Layout title="Academic Records"><div className="page-header"><div><div className="eyebrow">Student Portal</div><h1>Academic Records</h1><p>Track your marks, grades, and academic performance.</p></div></div><Card>{loading?<LoadingState/>:error?<ErrorState message={error}/>:r.length===0?<EmptyState text="No academic records found"/>:<div style={{overflowX:"auto"}}><table className="data-table"><thead><tr><th>Course</th><th>Internal</th><th>External</th><th>Total</th><th>Grade</th></tr></thead><tbody>{r.map((x,i)=><tr key={x._id||i}><td><div className="row-title">{x.course?.courseCode||x.courseCode}</div><div className="row-sub">{x.course?.courseName||x.courseName}</div></td><td>{x.internalMarks}</td><td>{x.externalMarks}</td><td>{x.totalMarks}</td><td><StatusPill tone={x.grade?.startsWith("A")?"good":"blue"}>{x.grade||"—"}</StatusPill></td></tr>)}</tbody></table></div>}</Card></Layout>}
