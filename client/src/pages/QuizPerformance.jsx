import React,{useEffect,useState}from"react";import Layout from"../components/Layout";import{Card,StatCard,LoadingState,ErrorState,StatusPill}from"../components/UI";import{ClipboardList,TrendingUp}from"lucide-react";import{apiGet}from"../api";
export default function QuizPerformance(){const[r,setR]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{apiGet("/quiz-performance/student").then(d=>setR(d.quizPerformance||[])).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);const avg=r.length?Math.round(r.reduce((a,x)=>a+Number(x.percentage||0),0)/r.length):0;return <Layout title="Quizzes"><div className="page-header"><div><div className="eyebrow">Student Portal</div><h1>Quiz Performance</h1><p>Review your quiz attempts and assessment scores.</p></div></div><div className="stats-grid" style={{gridTemplateColumns:"repeat(2,minmax(0,1fr))"}}><StatCard title="Quizzes Attempted" value={r.length} note="Completed assessments" icon={ClipboardList} tone="blue"/><StatCard title="Average Score" value={`${avg}%`} note="Overall quiz performance" icon={TrendingUp} tone="green"/></div><div style={{marginTop:11}}><Card title="Quiz Results" subtitle="Your recent assessments">{loading?<LoadingState/>:error?<ErrorState message={error}/>:<div style={{overflowX:"auto"}}><table className="data-table"><thead><tr><th>Quiz</th><th>Course</th><th>Score</th><th>Percentage</th><th>Performance</th><th>Date</th></tr></thead><tbody>{r.map((x,i)=><tr key={x._id||i}><td className="row-title">{x.quizTitle}</td><td>{x.courseCode}</td><td>{x.score}/{x.totalMarks}</td><td>{x.percentage}%</td><td><StatusPill tone={Number(x.percentage)>=75?"good":Number(x.percentage)>=50?"warning":"danger"}>{x.performanceLevel||"—"}</StatusPill></td><td>{x.attemptedAt?new Date(x.attemptedAt).toLocaleDateString():"—"}</td></tr>)}</tbody></table></div>}</Card></div></Layout>}
