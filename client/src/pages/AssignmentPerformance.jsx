import React,{useEffect,useState}from"react";import Layout from"../components/Layout";import{Card,StatCard,LoadingState,ErrorState,StatusPill}from"../components/UI";import{FileCheck2,TrendingUp}from"lucide-react";import{apiGet}from"../api";
export default function AssignmentPerformance(){const[r,setR]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{apiGet("/assignment-performance/student").then(d=>setR(d.assignmentPerformance||[])).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);const avg=r.length?Math.round(r.reduce((a,x)=>a+(Number(x.marksObtained||0)/Number(x.totalMarks||1))*100,0)/r.length):0;return <Layout title="Assignments"><div className="page-header"><div><div className="eyebrow">Student Portal</div><h1>Assignment Performance</h1><p>Track submissions, marks, and faculty feedback.</p></div></div><div className="stats-grid" style={{gridTemplateColumns:"repeat(2,minmax(0,1fr))"}}><StatCard title="Assignments" value={r.length} note="Tracked submissions" icon={FileCheck2} tone="blue"/><StatCard title="Average Score" value={`${avg}%`} note="Across graded work" icon={TrendingUp} tone="purple"/></div><div style={{marginTop:11}}><Card title="Assignment Results" subtitle="Recent submissions">{loading?<LoadingState/>:error?<ErrorState message={error}/>:<div style={{overflowX:"auto"}}><table className="data-table"><thead><tr><th>Assignment</th><th>Course</th><th>Marks</th><th>Status</th><th>Performance</th><th>Feedback</th></tr></thead><tbody>{r.map((x,i)=><tr key={x._id||i}><td className="row-title">{x.assignmentTitle}</td><td>{x.courseCode}</td><td>{x.marksObtained}/{x.totalMarks}</td><td><StatusPill tone={x.status==="Graded"?"good":"blue"}>{x.status}</StatusPill></td><td><StatusPill tone={x.performanceLevel==="Excellent"?"good":"warning"}>{x.performanceLevel||"—"}</StatusPill></td><td>{x.feedback||"—"}</td></tr>)}</tbody></table></div>}</Card></div></Layout>}
