import React,{useEffect,useState}from"react";import Layout from"../components/Layout";import{Card,StatCard,LoadingState,ErrorState,Progress,StatusPill}from"../components/UI";import{CalendarCheck,AlertTriangle}from"lucide-react";import{apiGet}from"../api";
export default function Attendance(){const[r,setR]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{apiGet("/course-attendance/student").then(d=>setR(d.courseAttendance||[])).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);const avg=r.length?Math.round(r.reduce((a,x)=>a+Number(x.attendancePercentage||0),0)/r.length):0;return <Layout title="Attendance"><div className="page-header"><div><div className="eyebrow">Student Portal</div><h1>Attendance</h1><p>Monitor your subject-wise attendance.</p></div></div><div className="stats-grid" style={{gridTemplateColumns:"repeat(2,minmax(0,1fr))"}}><StatCard title="Overall Attendance" value={`${avg}%`} note="Across current courses" icon={CalendarCheck} tone={avg<75?"orange":"green"}/><StatCard title="Subjects Below 75%" value={r.filter(x=>Number(x.attendancePercentage)<75).length} note="Need attention" icon={AlertTriangle} tone="blue"/></div><div style={{marginTop:11}}><Card title="Subject-wise Attendance" subtitle="Current attendance status">{loading?<LoadingState/>:error?<ErrorState message={error}/>:<div className="risk-list">{r.map(x=><div className="risk-row" key={x.courseCode}><div className="risk-avatar">{x.courseCode?.slice(-1)}</div><div className="risk-main"><strong>{x.courseCode} · {x.courseName}</strong><span>{x.presentClasses} present / {x.totalClasses} classes</span><Progress value={x.attendancePercentage} tone={Number(x.attendancePercentage)<75?"red":"green"}/></div><div><div className="risk-value">{x.attendancePercentage}%</div><StatusPill tone={Number(x.attendancePercentage)<75?"danger":"good"}>{x.status||"Good"}</StatusPill></div></div>)}</div>}</Card></div></Layout>}
