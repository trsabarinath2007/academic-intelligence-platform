import React,{useState}from"react";import{Link,useNavigate}from"react-router-dom";import{GraduationCap}from"lucide-react";import{apiPost}from"../api";
export default function Login(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[loading,setLoading]=useState(false);const navigate=useNavigate();const submit=async e=>{e.preventDefault();try{setLoading(true);setError("");const d=await apiPost("/auth/login",{email,password});localStorage.setItem("token",d.token);localStorage.setItem("user",JSON.stringify(d.user));const r=d.user?.role;navigate(r==="admin"?"/admin-dashboard":r==="faculty"?"/faculty-dashboard":"/student-dashboard")}catch(err){setError(err.message)}finally{setLoading(false)}};return <div className="auth-page"><div className="auth-visual"><div className="auth-brand"><div className="brand-mark"><GraduationCap size={24}/></div><strong>Academic<br/>Intelligence</strong></div><div className="auth-copy"><div className="eyebrow">Smart Academic Platform</div><h1>Turn academic data into intelligent decisions.</h1><p>A unified workspace for administrators, faculty, and students to understand academic performance.</p></div><small style={{color:"#8a97aa",fontSize:10}}>MERN · Analytics · Academic Intelligence</small></div><div className="auth-card-wrap"><div className="auth-card"><h2>Welcome back</h2><p>Sign in to continue to your portal.</p><form className="auth-form" onSubmit={submit}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required placeholder="you@example.com"/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required placeholder="Enter password"/></label>{error&&<div className="auth-error">{error}</div>}<button className="auth-button" disabled={loading}>{loading?"Signing in...":"Sign In"}</button></form><div className="auth-link">Need an account? <Link to="/">Return to home</Link></div></div></div></div>}
